apiVersion: v1
kind: Service
metadata:
  name: webhook-event-source-github-push
  namespace: argo-events
spec:
  type: NodePort
  selector:
    eventsource-name: webhook-event-source
    eventsource-type: webhook
  ports:
    - port: 12000
      targetPort: 12000
      nodePort: 30120

