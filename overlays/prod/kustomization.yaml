apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: prod

resources:
  - ../../base

patches:
  - path: rollout-patch.yaml
  - path: hpa-patch.yaml
  - path: ingress-patch.yaml
  - path: networkpolicy.yaml

images:
  - name: 192.168.30.11:30003/library/mcp-hr-server:latest
    newTag: v1.0.0

configMapGenerator:
  - name: myapp-config
    behavior: merge
    literals:
      - app_env=production
      - log_level=warn
      - max_requests=5000
