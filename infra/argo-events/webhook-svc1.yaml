apiVersion: v1
kind: Service
metadata:
  name: webhook-event-source-github-push
  namespace: argo-events1
spec:
  type: NodePort
  selector:
    owner-name: webhook-event-source
  ports:
    - port: 12000
      targetPort: 12000
      nodePort: 30120
